import { PmUiLazySusanItemDirective } from './pm-ui-lazy-susan-item.directive';

describe('PmUiLazySusanItemDirective', () => {
  it('should create an instance', () => {
    const directive = new PmUiLazySusanItemDirective();
    expect(directive).toBeTruthy();
  });
});
